apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: color-app
  labels:
    app: color-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: color-app
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
